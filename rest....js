//1
//Write a function with rest parameters to print sightings 
// of different animals within the sanctuary. 
// This function should accept an arbitrary number of animal names.
function animals(...names) {
    console.log(names);
    return names.animals((sum) => sum + 1)
};
console.log(names, sum);
//2
